node_modules/
dist/
.env
.env.local
codex-config/config.toml
codex-config/
codex-config;C/

# Working notes — kept out of version control because the file holds
# host/DB metadata copied from local .env files and would otherwise drift.
PROGRESS.md

# prepare-skills copies these from ../../skills-project/ at build time.
# Keep them out of git to avoid drift with the source.
skills/
scripts/memory-read.ts
scripts/memory-write.ts
scripts/memory-delete.ts
