# Sandbox

This directory is the execution sandbox for the Claude Code agent.

When a release decision workflow is triggered from the web UI, the agent runs inside this isolated environment — reading skills, writing experiment files, and producing analysis outputs.
