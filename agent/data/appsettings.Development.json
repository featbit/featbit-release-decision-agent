{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },

  // Override to use local TSDB when running docker compose with local tsdb service:
  // "TsdbProvider": "local"
  "TsdbProvider": "cloudflare"
}
