apiVersion: v2
name: featbit-rda
description: |
  Helm chart for the FeatBit Release Decision Agent.
  Umbrella chart that deploys the services in modules/* to Kubernetes.
  Each service is toggled via its own section in values.yaml (e.g. trackService.enabled).
type: application
version: 0.0.3
appVersion: "0.0.5-beta"
home: https://github.com/featbit/featbit-release-decision-agent
sources:
  - https://github.com/featbit/featbit-release-decision-agent
maintainers:
  - name: FeatBit
